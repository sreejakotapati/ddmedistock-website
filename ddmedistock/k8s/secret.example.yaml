# TEMPLATE ONLY — do not commit real secrets. Apply via a sealed-secret,
# external-secrets operator, or `kubectl create secret` in your cluster.
#
#   kubectl -n ddmedistock create secret generic ddmedistock-secrets \
#     --from-literal=DATABASE_URL='postgresql://USER:PASS@HOST:5432/ddmedistock?schema=public&sslmode=require' \
#     --from-literal=JWT_SECRET='<long-random-string>' \
#     --from-literal=OPENAI_API_KEY='sk-...'
apiVersion: v1
kind: Secret
metadata:
  name: ddmedistock-secrets
  namespace: ddmedistock
type: Opaque
stringData:
  DATABASE_URL: "postgresql://ddmedistock:ddmedistock@ddmedistock-db:5432/ddmedistock?schema=public"
  JWT_SECRET: "change-me-to-a-long-random-string"
  OPENAI_API_KEY: ""
